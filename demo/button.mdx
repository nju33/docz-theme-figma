---
name: Button
menu: Examples
route: /atom/example/button
---

import {Playground} from 'docz';
import Button from './button';

# Button

<Playground>
  <div style={{padding: 10}}>
    <div>
      <Button>START</Button>
    </div>
    <div style={{marginTop: 10}}>
      <Button className="transparency">START</Button>
    </div>
    <div style={{marginTop: 10, display: 'flex'}}>
      <Button className="block">START</Button>
      <Button className="block" style={{marginLeft: 10}}>START</Button>
    </div>
  </div>
</Playground>
